# Port the API listens on (frontend expects 3001 by default)
PORT=3001

# Node environment: "development" enables request logging
NODE_ENV=development

# Path to the SQLite database file (relative to the backend folder)
DATABASE_PATH=data.sqlite
