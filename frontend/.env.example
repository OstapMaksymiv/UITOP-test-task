# Base URL of the backend REST API. Must be prefixed with NEXT_PUBLIC_ so it is
# available in the browser. Point this at your deployed backend in production.
NEXT_PUBLIC_API_URL=https://task-api.onrender.com
